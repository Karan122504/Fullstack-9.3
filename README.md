# Fullstack-9.3
Build a React frontend and Node.js/Express backend with optional MongoDB. Deploy frontend and backend on separate EC2 instances. Use an Application Load Balancer to distribute traffic across backend servers. Configure VPC, security groups, and test via browser for proper load handling.
